# Almost Due 📅

一款可爱的保质期管理应用，帮助你追踪食品和物品的有效期，避免浪费。

[English](./README_EN.md) | 简体中文

## ✨ 功能特性

- **🧸 清新可爱的界面** - 精心设计的 UI，让管理保质期变得有趣
- **📝 手动添加物品** - 快速录入物品名称、到期日期和备注
- **🤖 AI 智能识别** - 支持通过 AI 自动识别和添加物品信息
- **⏰ 到期提醒** - 可自定义提前多少天提醒（默认 3 天）
- **📊 状态概览** - 首页展示已过期、即将过期和安全物品的数量统计
- **💾 本地存储** - 数据安全存储在本地设备

## 🛠️ 技术栈

- **框架**: [Flutter](https://flutter.dev/) 3.10+
- **状态管理**: [Riverpod](https://riverpod.dev/) 3.x (with code generation)
- **路由**: [go_router](https://pub.dev/packages/go_router)
- **本地存储**: [shared_preferences](https://pub.dev/packages/shared_preferences)
- **字体**: [Google Fonts](https://pub.dev/packages/google_fonts)

## 📁 项目结构

```
lib/
├── app/              # 应用配置、路由、主题
├── data/
│   ├── models/       # 数据模型 (ExpiryItem, AppSettings)
│   └── services/     # 数据服务 (StorageService)
├── state/            # Riverpod 状态管理
├── ui/
│   ├── screens/      # 页面 (首页、添加、设置)
│   └── widgets/      # 可复用组件
├── utils/            # 工具函数
└── main.dart         # 入口文件
```

## 🚀 快速开始

### 环境要求

- Flutter SDK 3.10.4 或更高版本
- Dart SDK 3.0 或更高版本

### 安装与运行

1. **克隆项目**

   ```bash
   git clone <repository-url>
   cd almost_due_app
   ```

2. **安装依赖**

   ```bash
   flutter pub get
   ```

3. **生成代码** (Riverpod)

   ```bash
   dart run build_runner build --delete-conflicting-outputs
   ```

4. **运行应用**
   ```bash
   flutter run
   ```

### 开发模式

启用代码生成监听，实时更新：

```bash
dart run build_runner watch --delete-conflicting-outputs
```

## ⚙️ 配置说明

### AI 功能

在设置页面配置 AI 接口：

- **API Base URL**: 你的 AI 服务地址
- **API Key**: 对应的 API 密钥

### 提醒天数

可在设置中调整提前提醒的天数，默认为 3 天。

## 📱 支持平台

- ✅ Android
- ✅ iOS
- ✅ Web
- ✅ macOS
- ✅ Windows
- ✅ Linux

## 📄 许可证

MIT License
